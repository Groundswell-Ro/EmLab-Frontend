<?xml version="1.0" encoding="utf-8"?>
<messages>

    <message id="home-sidebar">
        ${sidebar-toggler class="absolute top-0 -right-14 flex justify-center items-center btn btn-primary p-1.5 rounded-full animate-spacing shadow-md"}
        ${calendar class="calendar w-full"}
        <div class="relative mb-10">
            ${add-event-btn class="btn btn-primary absolute top-0 left-0 animate-all"}
            ${save-event-btn class="btn btn-primary absolute top-0 left-0 animate-all -translate-x-28"}
            ${selected-date class="absolute top-2 right-12 underline underline-offset-4"}
            ${cancel-event-btn class="btn btn-primary rounded-full p-1 absolute top-0 right-0 transition-opacity ease-in-out duration-300 delay-75 disabled:opacity-0 disabled:cursor-default"}
        </div>
        ${sidebar-content class="w-full animate-width shadow-lg"}
                  
    </message>

    <message id='sidebar-event-open'>
        <div class="relative shadow-lg bg-body bor-1 m-1 p-1 py-5">
            ${toggle-size-btn class="bg-transparent shadow-none absolute top-0 right-0 p-1 m-0 flex justify-center items-center rounded-md border-none"}
            ${event-view}
        </div>
    </message>

    <message id='event-view'>
        ${event-data-panel}
    </message>

    <message id='event-form'>
        <div class="w-full">

            <div class="flex mb-2">
                <span class="w-28 flex items-center">${clock-svg class="flex justify-center items-center me-1"}Start Time</span>
                <span class="flex">
                ${start-sub-time-btn class="btn btn-primary p-0.5 m-0 rounded-none flex justify-center items-center"}
                ${start-time-input class="input-underline border-t w-16 px-1 text-center"}
                ${start-add-time-btn class="btn btn-primary p-0.5 m-0 rounded-none flex justify-center items-center"}
                </span>
            </div>

            <div class="flex mb-2">
                <span class="w-28 flex items-center">${hourglass-svg class="flex justify-center items-center me-1"}Duration</span>
                <div class="flex">
                ${duration-sub-time-btn class="btn btn-primary p-0.5 m-0 rounded-none flex justify-center items-center"}
                ${duration-time-input class="input-underline border-t w-16 px-1 text-center"}
                ${duration-add-time-btn class="btn btn-primary p-0.5 m-0 rounded-none flex justify-center items-center"}
                </div>
            </div>

            <div class="flex mb-3">
                <span class="w-28 flex items-center">${clock-svg-fill class="flex justify-center items-center me-1"}End Time</span>
                <span class="flex ms-7 text-sm cursor-default">${end-hour}</span>
            </div>

            <div class="flex flex-col items-stretch relative mb-3">
                ${location-input class="input-float peer"} 
                <label for="${id:location-input}" class="label-float flex items-center">${location-pin-svg class="flex justify-center items-center me-1"}Location</label>
            </div>

            <div class="flex flex-col items-stretch relative">
                ${description-input class="input-float peer resize-y"} 
                <label for="${id:description-input}" class="label-float flex items-center">${book-open-svg class="flex justify-center items-center me-1"}Description</label>
            </div>
        </div>
    </message>

    <message id='event-time-input'>
        ${minus}
        ${input}
        ${plus}
    </message>

</messages>
